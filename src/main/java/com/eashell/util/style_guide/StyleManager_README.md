# StyleManager.java - Довідник по стилізації

## 🎨 Кольорова палітра (натхненна Keqing з Genshin Impact)

### Основні фонові кольори
```java
PRIMARY_BG = "#1a1425"      // Найтемніший фон (основний)
SECONDARY_BG = "#241b2f"    // Трохи світліший (панелі)
CARD_BG = "#2d2139"         // Фон карток скриптів
CARD_HOVER_BG = "#3a2847"   // Фон картки при наведенні миші
```

### Акцентні кольори (фіолетові/електричні відтінки)
```java
ACCENT_PURPLE = "#9d7bd8"   // Основний фіолетовий
ACCENT_ELECTRIC = "#b8a4e8" // Світло-фіолетовий (електро ефект)
ACCENT_GOLD = "#d4af37"     // Золотий акцент
ACCENT_GREEN = ACCENT_ELECTRIC  // Зелений замінено на фіолетовий
ACCENT_BLUE = ACCENT_PURPLE     // Синій замінено на фіолетовий
ACCENT_PINK = "#e89fd9"     // Рожевий
ACCENT_RED = "#e85d75"      // Червоний
```

### Рамки та свічення
```java
BORDER_COLOR = "#4a3960"    // Звичайна рамка
BORDER_GLOW = "#8b6bb8"     // Рамка зі світінням (при наведенні/фокусі)
```

### Кольори тексту
```java
TEXT_PRIMARY = "#e8dff5"    // Основний текст (білий з фіолетовим відтінком)
TEXT_SECONDARY = "#b8a4e8"  // Вторинний текст (світло-фіолетовий)
TEXT_MUTED = "#7a6b8f"      // Приглушений текст (сірий)
TEXT_ACCENT = "#d4af37"     // Акцентний текст (золотий)
```

---

## 📐 Структура UI елементів

### 1. Верхня панель (TopBar)
- **Фон**: Градієнт від CARD_BG до #3d2952
- **Рамка**: Фіолетова лінія знизу (BORDER_GLOW)
- **Ефект**: Тінь з світінням

### 2. Картки скриптів (ScriptCard)
- **Фон**: Градієнт від CARD_BG до #261c33
- **Рамка**: Темно-фіолетова 1.5px
- **Hover ефект**: Світліший фон + яскравіша рамка (BORDER_GLOW)
- **Тінь**: Зелено-фіолетове світіння

### 3. Панелі (ScriptListPanel, OutputPanel)
- **ScriptListPanel**: Градієнт від SECONDARY_BG до PRIMARY_BG
- **OutputPanel**: Градієнт від PRIMARY_BG до #120e1a (найтемніший)

### 4. Консольна область (TextArea в OutputPanel)
- **Фон**: #0d0a12 (майже чорний)
- **Текст**: ACCENT_ELECTRIC (світло-фіолетовий)
- **Шрифт**: Monospace (Consolas)

### 5. Кнопки

#### Великі кнопки (TopBar)
- Градієнт фон
- При наведенні: світліший + збільшення 1.05x
- Тінь зі світінням

#### Маленькі кнопки (Картки, OutputPanel)
- Однотонний фон
- При наведенні: світліший колір

---

## 🎯 Де застосовуються кольори

### ACCENT_GREEN (насправді фіолетовий)
- Кнопка "+ NEW SCRIPT"
- Кнопка "▶ RUN" на картках
- Індикатор "🟢" коли скрипт працює

### ACCENT_RED (червоний)
- Кнопка "⏹ STOP ALL"
- Кнопка "⏹ STOP" у вкладках
- Кнопка "✖ DELETE" на картках

### ACCENT_BLUE (насправді фіолетовий)
- Кнопка "✎ EDIT" на картках
- Кнопка "🗑 CLEAR" у вкладках
- Кнопка "Browse" в діалозі

---

## 📝 Методи для роботи зі стилями

### Отримання стилів для контейнерів
```java
getRootStyle()           // Фон головного вікна
getTopBarStyle()         // Стиль верхньої панелі
getPanelStyle()          // Стиль лівої панелі
getOutputPanelStyle()    // Стиль правої панелі
getCardStyle()           // Стиль картки скрипта
getCardHoverStyle()      // Стиль картки при наведенні
```

### Створення кнопок
```java
createStyledButton(text, color)  // Велика кнопка (TopBar)
createSmallButton(text, color)   // Маленька кнопка (картки, вкладки)
```

### Стилізація форм
```java
styleTextField(field)    // Стиль для TextField
styleTextArea(area)      // Стиль для TextArea
createLabel(text)        // Створення Label з стилем
```

### Індикатори статусу
```java
setRunningStatus(label)  // Встановлює 🟢 зі світінням
setStoppedStatus(label)  // Встановлює ⚫ без світіння
```

---

## ✨ Ефекти

### DropShadow (Тінь зі світінням)
Використовується для:
- Верхньої панелі
- Карток скриптів
- Кнопок
- Заголовків

### Градієнти
Використовуються для:
- Фонів панелей
- Кнопок
- Тексту заголовка

### Hover ефекти
- Картки: світліший фон + яскравіша рамка
- Кнопки: світліший колір + збільшення
- Поля вводу: фіолетова рамка + світіння

---

## 🔤 Шрифти

```java
FONT_ELEGANT = "'Segoe UI', 'Helvetica Neue', sans-serif"  // Для UI
FONT_MONO = "'Consolas', 'Courier New', monospace"         // Для коду/консолі
```

**Використання:**
- FONT_ELEGANT: весь UI (кнопки, заголовки, тексти)
- FONT_MONO: команди на картках, консольний вивід