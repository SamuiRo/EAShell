# ⚡ Швидкий довідник EA Shell UI

## 📁 Файли та їх призначення

| Файл | За що відповідає |
|------|------------------|
| `MainWindow.java` | Головна логіка програми, об'єднання всіх компонентів |
| `TopBar.java` | Верхня панель: назва, лічильник, кнопки |
| `ScriptListPanel.java` | Ліва панель: список карток скриптів |
| `ScriptCard.java` | Одна картка скрипта в списку |
| `OutputPanel.java` | Права панель: вкладки з консольним виводом |
| `ScriptDialog.java` | Діалог додавання/редагування скрипта |
| `DeleteConfirmDialog.java` | Діалог підтвердження видалення |
| `StyleManager.java` | ВСІ кольори, шрифти, CSS стилі |
| `Constants.java` | Розміри, тексти, таймаути |

---

## 🎨 Де знайти що змінити

### Хочу змінити КОЛІР
→ **StyleManager.java** → Константи на початку файлу

### Хочу змінити РОЗМІР ВІКНА
→ **Constants.java** → `WINDOW_WIDTH`, `WINDOW_HEIGHT`

### Хочу змінити ТЕКСТ (заголовки, кнопки)
→ **Constants.java** → Константи з текстами

### Хочу змінити РОЗТАШУВАННЯ елементів
→ Відповідний **Panel.java** або **Card.java** → Методи ініціалізації

### Хочу додати ЕФЕКТ (тінь, градієнт)
→ **StyleManager.java** → Відповідний метод `getXxxStyle()`

---

## 🔍 Швидкий пошук елементів

### "Де код для верхньої панелі?"
→ `TopBar.java` → `initialize()`

### "Де код для картки скрипта?"
→ `ScriptCard.java` → `initializeCard()`

### "Де код для консольного виводу?"
→ `OutputPanel.java` → `createOutputTab()`

### "Де код для діалогу додавання скрипта?"
→ `ScriptDialog.java` → `showAddDialog()`

### "Де код для кнопки RUN?"
→ `ScriptCard.java` → `createButtonBox()` → `runBtn`

### "Де обробляється натискання RUN?"
→ `MainWindow.java` → `handleRunScript()`

---

## 🎯 Найчастіші зміни

### Змінити колір кнопки
```java
// StyleManager.java
public static final String ACCENT_GREEN = "#00ff41";
```

### Змінити розмір шрифту
```java
// StyleManager.java → відповідний метод
"-fx-font-size: 16px;" // Було 14px
```

### Змінити відступи
```java
// В відповідному класі
setSpacing(15); // Було 10
setPadding(new Insets(25)); // Було 20
```

### Змінити час оновлення
```java
// TopBar.java → startStatusUpdater()
}, 0, 500); // Було 1000 (мілісекунди)
```

### Додати тінь
```java
// StyleManager.java
"-fx-effect: dropshadow(gaussian, rgba(0,0,0,0.5), 10, 0.5, 0, 2);"
```

---

## 🔤 Структура імен методів

### getXxxStyle()
Повертає CSS стиль для елемента
```java
getCardStyle()        // Стиль картки скрипта
getButtonStyle()      // Стиль кнопки
getHeaderStyle()      // Стиль заголовка
```

### createXxx()
Створює UI елемент
```java
createTitleBox()      // Створює рядок з назвою
createButtonBox()     // Створює панель з кнопками
createOutputTab()     // Створює вкладку виводу
```

### handleXxx()
Обробляє дію користувача
```java
handleAddScript()     // Обробляє додавання скрипта
handleRunScript()     // Обробляє запуск скрипта
handleStopAll()       // Обробляє зупинку всіх
```

### updateXxx()
Оновлює стан UI
```java
updateScriptStatus()  // Оновлює статус (⚫/🟢)
refreshScriptList()   // Оновлює список карток
```

---

## 📊 Структура даних

### Мапа активних процесів
```java
Map<String, ProcessRunner> runningProcesses
// "My Script" → ProcessRunner об'єкт
// "Build App" → ProcessRunner об'єкт
```

### Мапа карток скриптів
```java
Map<String, ScriptCard> scriptCards
// "My Script" → ScriptCard об'єкт
// "Build App" → ScriptCard об'єкт
```

### ScriptEntry (модель даних)
```java
{
    name: "My Script",
    workingDir: "/path/to/project",
    commands: ["npm install", "npm start"]
}
```

---

## 🎨 Колірна палітра (швидкий доступ)

```
Фони:
#0d0a12 ← Консоль (найтемніший)
#1a1425 ← Основний фон
#2d2139 ← Картки скриптів

Акценти:
#b8a4e8 ← Фіолетовий світлий (кнопки, текст)
#9d7bd8 ← Фіолетовий темний
#e85d75 ← Червоний (DELETE, STOP)

Текст:
#e8dff5 ← Основний текст
#7a6b8f ← Сірий текст

Рамки:
#4a3960 ← Звичайна рамка
#8b6bb8 ← Рамка зі світінням
```

---

## ⚙️ Розміри (швидкий доступ)

```
Вікно: 1400 x 800 пікселів
Поділ панелей: 40% ліва, 60% права

Відступи:
- TopBar: 15px padding, 20px spacing
- Panels: 20px padding, 10px spacing
- Cards: 15px padding, 8px spacing

Шрифти:
- Заголовок TopBar: 26px
- Заголовок панелей: 18px
- Назва картки: 16px
- Команди: 12px
- Кнопки: 12px (великі), 10px (маленькі)
```

---

## 🔗 Зв'язки між класами

```
MainWindow
├── TopBar
│   ├── onAddScript → MainWindow.handleAddScript()
│   ├── onStopAll → MainWindow.handleStopAll()
│   └── runningCountSupplier → runningProcesses.size()
├── ScriptListPanel
│   └── ScriptCard (багато)
│       ├── onRun → MainWindow.handleRunScript()
│       ├── onEdit → MainWindow.handleEditScript()
│       └── onDelete → MainWindow.handleDeleteScript()
└── OutputPanel
    └── Tab (багато)
        ├── TextArea (консольний вивід)
        └── [STOP] [CLEAR] кнопки
```

---

## 🐛 Дебаг: Де шукати проблему

### Картка не відображається
→ `ScriptListPanel.refresh()` → Перевір чи додається в `scriptListContainer`

### Кнопка не працює
→ Відповідний клас → `setOnAction()` → Перевір чи викликається колбек

### Вивід не з'являється в консолі
→ `OutputPanel.createOutputTab()` → Перевір чи `outputArea` правильно передається в `ProcessRunner`

### Статус не оновлюється
→ `MainWindow.updateScriptStatus()` → Перевір чи викликається `Platform.runLater()`

### Колір не змінюється
→ `StyleManager` → Перевір чи константа використовується в правильному методі

---

## 💡 Підказки для початківців

1. **Завжди перезапускай програму після змін** - JavaFX не має hot reload

2. **Використовуй Ctrl+F для пошуку** - швидше знайдеш потрібний код

3. **Коментуй свої зміни** - потім легше згадати що і чому змінив

4. **Починай з малого** - спочатку зміни колір, потім розмір, потім складніше

5. **Дивись на консоль** - там можуть бути помилки якщо щось не працює

6. **Роби бекапи** - скопіюй старе значення в коментар перед зміною

7. **Експериментуй** - найкращий спосіб навчитися!

---

## 📚 Корисні посилання

- JavaFX CSS Reference: https://openjfx.io/javadoc/17/javafx.graphics/javafx/scene/doc-files/cssref.html
- JavaFX Tutorial: https://docs.oracle.com/javafx/2/get_started/jfxpub-get_started.htm
- CSS Color Picker: https://htmlcolorcodes.com/

---

## ✅ Чек-лист для нової UI функції

- [ ] Визначив в якому файлі робити зміни
- [ ] Зробив бекап старого коду
- [ ] Протестував зміни
- [ ] Перевірив чи не зламалося щось інше
- [ ] Додав коментарі якщо потрібно
- [ ] Задокументував зміни (якщо великі)