# 🎨 Практичні приклади змін дизайну

## 🔧 Приклад 1: Змінити колір кнопки RUN

**Завдання:** Зробити кнопку RUN зеленою замість фіолетової

**Де змінювати:** `StyleManager.java`

```java
// БУЛО:
public static final String ACCENT_GREEN = ACCENT_ELECTRIC; // Фіолетовий

// СТАЛО:
public static final String ACCENT_GREEN = "#00ff41"; // Яскраво-зелений
```

**Що зміниться:**
- Кнопка "▶ RUN" стане зеленою
- Кнопка "+ NEW SCRIPT" стане зеленою
- Індикатор 🟢 залишиться з фіолетовим світінням (окремий метод)

---

## 🔧 Приклад 2: Змінити розмір вікна

**Завдання:** Зробити вікно більшим 1600x900 замість 1400x800

**Де змінювати:** `Constants.java`

```java
// БУЛО:
public static final int WINDOW_WIDTH = 1400;
public static final int WINDOW_HEIGHT = 800;

// СТАЛО:
public static final int WINDOW_WIDTH = 1600;
public static final int WINDOW_HEIGHT = 900;
```

**Що зміниться:**
- Вікно стане більшим при запуску
- Всі панелі автоматично розтягнуться пропорційно

---

## 🔧 Приклад 3: Змінити пропорції панелей

**Завдання:** Зробити ліву панель ширшою (50% замість 40%)

**Де змінювати:** `Constants.java`

```java
// БУЛО:
public static final double SPLIT_PANE_DIVIDER_POSITION = 0.4; // 40% ліва, 60% права

// СТАЛО:
public static final double SPLIT_PANE_DIVIDER_POSITION = 0.5; // 50% ліва, 50% права
```

**Що зміниться:**
- Ліва панель стане ширшою
- Права панель стане вужчою
- Користувач все ще може змінити це перетягуванням роздільника

---

## 🔧 Приклад 4: Змінити колір фону карток

**Завдання:** Зробити картки темніше

**Де змінювати:** `StyleManager.java`

```java
// БУЛО:
public static final String CARD_BG = "#2d2139";

// СТАЛО:
public static final String CARD_BG = "#1a1425"; // Як PRIMARY_BG
```

**Що зміниться:**
- Картки скриптів стануть темнішими
- Візуально "зіллються" з фоном лівої панелі

---

## 🔧 Приклад 5: Додати тінь до верхньої панелі

**Завдання:** Зробити більшу тінь під TopBar

**Де змінювати:** `StyleManager.java` → метод `getTopBarStyle()`

```java
// БУЛО:
"-fx-effect: dropshadow(gaussian, rgba(157, 123, 216, 0.4), 10, 0.5, 0, 2);"

// СТАЛО:
"-fx-effect: dropshadow(gaussian, rgba(157, 123, 216, 0.8), 20, 0.8, 0, 5);"
//                                                         ↑    ↑    ↑  ↑
//                                   прозорість 0.8 (більше)  |    |  зсув вниз 5px
//                                              радіус 20px ──┘    |
//                                                    spread 0.8 ──┘
```

**Параметри DropShadow:**
- `gaussian` - тип розмиття
- `rgba(...)` - колір тіні з прозорістю
- `радіус` - розмір тіні (більше = більша тінь)
- `spread` - щільність тіні (0-1)
- `offsetX` - зсув по горизонталі
- `offsetY` - зсув по вертикалі

---

## 🔧 Приклад 6: Змінити шрифт команд на картках

**Завдання:** Зробити команди більшими

**Де змінювати:** `StyleManager.java` → метод `getCardCommandStyle()`

```java
// БУЛО:
return "-fx-text-fill: " + TEXT_SECONDARY + ";" +
       "-fx-font-size: 12px;" +
       "-fx-font-family: " + FONT_MONO + ";";

// СТАЛО:
return "-fx-text-fill: " + TEXT_SECONDARY + ";" +
       "-fx-font-size: 14px;" + // Збільшено з 12px до 14px
       "-fx-font-family: " + FONT_MONO + ";";
```

---

## 🔧 Приклад 7: Змінити відступи між картками

**Завдання:** Зменшити пробіл між картками скриптів

**Де змінювати:** `ScriptListPanel.java` → конструктор

```java
// БУЛО:
scriptListContainer = new VBox(10); // 10px між картками

// СТАЛО:
scriptListContainer = new VBox(5); // 5px між картками
```

---

## 🔧 Приклад 8: Додати градієнт до кнопок

**Завдання:** Зробити кнопку STOP з градієнтом червоного

**Де змінювати:** `StyleManager.java` → метод `getSmallButtonStyle()`

```java
// БУЛО:
return "-fx-background-color: " + color + ";";

// СТАЛО:
return "-fx-background-color: linear-gradient(to bottom, " + color + ", derive(" + color + ", -20%));";
//                            ────────────────────────────   ──────   ─────────────────────────────
//                            градієнт зверху вниз           від color до темніше на 20%
```

**Що робить `derive(color, -20%)`:**
- Бере базовий колір
- Робить його темніше на 20% (мінус = темніше, плюс = світліше)

---

## 🔧 Приклад 9: Змінити колір консольного виводу

**Завдання:** Зробити вивід зеленим замість фіолетового

**Де змінювати:** `StyleManager.java` → метод `getOutputAreaStyle()`

```java
// БУЛО:
return "-fx-control-inner-background: #0d0a12;" +
       "-fx-text-fill: " + ACCENT_ELECTRIC + ";";

// СТАЛО:
return "-fx-control-inner-background: #0d0a12;" +
       "-fx-text-fill: #00ff41;"; // Зелений як в терміналі
```

---

## 🔧 Приклад 10: Додати анімацію при наведенні на картку

**Завдання:** Картка повинна трохи збільшуватися при наведенні

**Де змінювати:** `ScriptCard.java` → метод `setupHoverEffect()`

```java
// БУЛО:
private void setupHoverEffect() {
    setOnMouseEntered(e -> setStyle(StyleManager.getCardHoverStyle()));
    setOnMouseExited(e -> setStyle(StyleManager.getCardStyle()));
}

// СТАЛО:
private void setupHoverEffect() {
    setOnMouseEntered(e -> {
        setStyle(StyleManager.getCardHoverStyle());
        setScaleX(1.02); // Збільшення по горизонталі на 2%
        setScaleY(1.02); // Збільшення по вертикалі на 2%
    });
    setOnMouseExited(e -> {
        setStyle(StyleManager.getCardStyle());
        setScaleX(1.0); // Повернення до нормального розміру
        setScaleY(1.0);
    });
}
```

---

## 🔧 Приклад 11: Змінити час оновлення лічильника

**Завдання:** Оновлювати "Running: X" кожні 500ms замість 1000ms

**Де змінювати:** `TopBar.java` → метод `startStatusUpdater()`

```java
// БУЛО:
}, 0, 1000); // 1000ms = 1 секунда

// СТАЛО:
}, 0, 500); // 500ms = 0.5 секунди
```

---

## 🔧 Приклад 12: Додати border до вкладок консолі

**Завдання:** Додати рамку навколо активної вкладки

**Де змінювати:** `StyleManager.java` → метод `getStylesheet()`

```java
// Додати в кінець getStylesheet():
".tab-pane .tab:selected { " +
"-fx-background-color: " + CARD_HOVER_BG + ";" +
"-fx-border-color: " + BORDER_GLOW + ";" +
"-fx-border-width: 0 0 3 0; }" // Рамка тільки знизу, 3px
```

**Що означають цифри в border-width:**
- `0 0 3 0` = верх, право, низ, ліво (в пікселях)
- Можна також `3` (всі сторони) або `3 5` (верх-низ, ліво-право)

---

## 🎯 Корисні CSS властивості для JavaFX

### Фони
```css
-fx-background-color: #ffffff;                    /* Однотонний */
-fx-background-color: linear-gradient(...);       /* Градієнт */
-fx-background-radius: 12;                        /* Закруглення */
```

### Рамки
```css
-fx-border-color: #ffffff;        /* Колір */
-fx-border-width: 2;              /* Товщина */
-fx-border-radius: 12;            /* Закруглення */
```

### Текст
```css
-fx-text-fill: #ffffff;           /* Колір тексту */
-fx-font-size: 14px;              /* Розмір шрифту */
-fx-font-weight: bold;            /* Жирність: normal, bold, 600 */
-fx-font-family: "Arial";         /* Шрифт */
-fx-font-style: italic;           /* Курсив */
```

### Ефекти
```css
-fx-effect: dropshadow(gaussian, rgba(0,0,0,0.5), 10, 0.5, 0, 2);
           /*          тип       колір+прозорість   радіус spread x  y */

-fx-effect: innershadow(...);     /* Внутрішня тінь */
```

### Трансформації (тільки через Java код)
```java
node.setScaleX(1.5);              // Збільшення по X
node.setScaleY(1.5);              // Збільшення по Y
node.setRotate(45);               // Поворот на 45°
node.setTranslateX(10);           // Зсув по X
```

---

## 📝 Чек-лист перед зміною дизайну

1. **Визнач що саме хочеш змінити:**
    - [ ] Колір елемента?
    - [ ] Розмір елемента?
    - [ ] Розташування елемента?
    - [ ] Ефект (тінь, градієнт)?
    - [ ] Анімація?

2. **Знайди відповідальний клас:**
    - Колір/стиль → `StyleManager.java`
    - Розміри/тексти → `Constants.java`
    - Розташування → Відповідний `*Panel.java` або `*Card.java`

3. **Зроби бекап:**
    - Скопіюй старе значення в коментар
    - Так легше повернутися назад якщо не сподобається

4. **Перевір результат:**
    - Перезапусти програму
    - Подивись на всі екрани де це використовується

5. **Документуй зміни:**
    - Додай коментар чому змінив
    - Особливо якщо зміна не очевидна